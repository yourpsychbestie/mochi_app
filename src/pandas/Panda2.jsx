import React from "react";

// SVG extraído fielmente del patrón, sin deformar
export default function Panda2({ size = 160, style = {} }) {
  return (
    <svg viewBox="0 0 200 200" width={size} height={size} style={style}>
      {/* Aquí irá el SVG del panda 2, extraído sin deformar */}
      {/* ...SVG PATHS... */}
    </svg>
  );
}
